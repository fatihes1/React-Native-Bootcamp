import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        padding: 10,
        flexDirection: "row",
        justifyContent: "space-between",
        alignItems: "center",
    },
    brandText: {
        fontSize: 25,
        fontWeight: "bold",
        color: "#ff5733",
    },
    todoLength: {
        fontSize: 25,
        fontWeight: "bold",
        color: "#ff5733",
    },

});