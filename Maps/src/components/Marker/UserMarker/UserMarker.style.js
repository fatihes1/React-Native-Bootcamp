import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  image: {
    width: 50,
    height: 50,
    borderRadius: 50,
    backgroundColor: 'white',
    borderColor: '#e0e0e0',
  },
});
