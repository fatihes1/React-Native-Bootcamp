import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    position: 'absolute',
    bottom: 300,
    alignSelf: 'center',
    padding: 12,
    backgroundColor: '#fff',
    borderRadius: 10,
    borderColor: '#e0e0e0',
  },
});
