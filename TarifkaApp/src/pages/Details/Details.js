import React from "react";
import {SafeAreaView, Text} from 'react-native'

const Details = () => {
    return (
        <SafeAreaView>
            <Text>
            Details
            </Text>
        </SafeAreaView>
    );
}

export default Details;